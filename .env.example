# Public - used by the browser and server
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Server only - used by /admin and /api routes to bypass RLS for inserts
SUPABASE_SERVICE_ROLE_KEY=

# Server only - single shared password gate for /admin
ADMIN_PASSWORD=change-me
